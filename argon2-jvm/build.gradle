apply plugin: 'java-library'

jar {
    manifest {
        attributes(
                'Automatic-Module-Name': 'de.mkammerer.argon2'
        )
    }
}

dependencies {
    api project(':argon2-jvm-nolibs')
}
